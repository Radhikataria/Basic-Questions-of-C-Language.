#include<stdio.h>
int main()
{

    printf("Size of integer type is=%d",sizeof(int));
    printf("\nSize of float type is=%d",sizeof(float));
    printf("\nSize of char type is=%d",sizeof(char));
    printf("\nSize of double type is=%d",sizeof(double));
    return 0;
}
